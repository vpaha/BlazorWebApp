<div id="user-container">
    <AuthorizeView>
        <Authorized Context="auth">
            <div>
                @{
                    var name = auth.User.Identity?.Name;
                    if (!string.IsNullOrEmpty(name))
                    {
                        <div>
                            Welcome, @name
                        </div>
                    }
                }
            </div>
            <div>
                <a href="authentication/signout" title="Sign Out">
                    <SfButton type="button" CssClass="e-outline e-info" Content="Sign Out" IconCss="e-icons e-export" IsPrimary="true" IconPosition="IconPosition.Right"></SfButton>
                </a>
            </div>
        </Authorized>
        <NotAuthorized>
            <div>
                Welcome, guest!
            </div>
            <div>
                <a href="requests" title="Sign in">
                    <SfButton type="button" CssClass="e-outline e-info" Content="Sign in" IconCss="e-icons e-import" IsPrimary="true" IconPosition="IconPosition.Right"></SfButton>
                </a>
            </div>
        </NotAuthorized>
    </AuthorizeView>
</div>
<style>
    #user-container {
        display: flex;
        gap: 10px;
        align-items: center;
    }
</style>